name: Pull Request
on: [pull_request]
jobs:
  PullRequest:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v3
      - name: Make a new envfile
        uses: SpicyPizza/create-envfile@v1
        with:
          envkey_DATABASE_URL: "postgresql://postgres:postgres@crud_doctors_db:5432/crud_doctors"
          envkey_DB_USER: "postgres"
          envkey_DB_PASSWORD: "postgres"
          envkey_DB_NAME: "crud_doctors"
          envkey_DB_HOST: "crud_doctors_db"
          envkey_DB_PORT: "5432"
          envkey_DB_CONNECTION: "postgresql"
          file_name: ".env.docker"
      - name: Run E2E tests
        run: docker-compose run crud_doctors_app npm run test:e2e
